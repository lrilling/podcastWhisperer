class Episode {
    constructor(id, name, series, file, transcriptFile) {
        this.id = id;
        this.name = name;
        this.series = series;
        this.file = file;
        this.transcriptFile = transcriptFile;
    }
}
