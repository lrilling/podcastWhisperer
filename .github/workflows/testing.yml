name: "Github Action Testing"
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: /home/runner/work/podcastWhisperer
    steps:
      - name: "Install Node"
        uses: actions/setup-node@v1
      - name: "Install Mocha"
        run: npm install -g mocha
      - name: "Install dependencies"
        run: npm install
      - name: "Run tests"
        run: npm test